<div class="w-full max-w-screen-xl  mx-auto p-4">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl"><%= t('home.menu.my_post') %></h1>
    <%= link_to t('home.header.new_post'), new_post_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="posts" class="min-w-full">
    <%= render @posts %>
  </div>
  <%= render 'layouts/components/pagination', posts: @posts %>
</div>