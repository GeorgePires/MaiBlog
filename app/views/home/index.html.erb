<section class="bg-white dark:bg-gray-900">
  <div class="container px-6 py-16 mx-auto ">
    <% @posts.each do |post| %>
      <div class="wrapper-small">
        <%= render post %>
      </div>
    <% end %>
  </div>
</section>
<%= render 'layouts/components/pagination', posts: @posts %>